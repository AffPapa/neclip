# Скриншоты — состояние реализации

Дата: 2026-09-10. Основа: `38f13e6fd041944a0ff531599d71bbe832d504b5`.
Локальный кандидат: **2.3.0/build 26**. Публичная версия остаётся **2.2.0/build 25**.

## Реализовано

- Переназначаемая ⌘⇧2 и команда «Скриншот области…» в меню.
- Одноразовый ScreenCaptureKit capture; выделение на экране с указателем, Escape, отмена при смене дисплеев.
- Пять инструментов: непрозрачное скрытие, перо, стрелка, рамка, текст.
- Нативное окно, undo/redo, масштаб по размеру / 100% / 200%, жестовое увеличение.
- Финальный PNG в буфер; системная панель сохранения PNG/JPEG; папка в существующих настройках.
- Плоский SDR/sRGB экспорт. Непрозрачные маски рисуются последними; исходные метаданные и слои не переносятся.
- Защита от изменения clipboard во время encode, own-write guard и явная запись только финального PNG в историю.
- История соблюдает паузу, ignore-next, исключения, image toggle, разрешение clipboard и лимиты. Save-only её не меняет.
- Исходник только в памяти одной сессии. Никакого фонового screen stream, OCR, облака или новой зависимости.
- Закрытие/выход защищены во время экспорта и при несохранённых пометках.

## Подтверждённые проверки

| Gate | Результат |
|---|---|
| Baseline | 247 XCTest, 4 skips, 0 failures; 3 Swift Testing |
| Текущий код, debug | 259 XCTest, 4 skips, 0 failures; 3 Swift Testing |
| Текущий код, strict release | 259 XCTest, 4 skips, 0 failures; 3 Swift Testing; complete concurrency + warnings-as-errors |
| TSan текущего среза | Все 12 screenshot tests PASS |
| ASan текущего среза | Все 12 screenshot tests PASS |
| Геометрия | Retina, независимые X/Y масштабы, отрицательный origin, drag/clamp, crop orientation |
| Скрытие | Разные секреты под одной маской дают одинаковые финальные PNG/JPEG |
| Экспорт на диск | PNG/JPEG реально записываются, декодируются, заменяют подтверждённый тестовый файл; ошибки не оставляют partial output |
| Clipboard | Проверка поколения, отсутствие file URL; PNG-only публикация, TIFF-конверсию рекламирует сама macOS |
| Native UI программно | Пять инструментов, команды, minimum size, drag/Escape, масштаб с учётом backingScaleFactor |
| Живой редактор | Все пять инструментов, русская подпись, ⌘Z/⌘⇧Z подтверждены визуально |
| Живое сохранение PNG | NSSavePanel → файл 720×420, 15 997 байт; все пометки и непрозрачная маска на месте |
| Живое копирование | ⌘Return → отдельный QA clipboard, PNG 720×420, 15 541 байт; рабочий clipboard не изменялся |
| Секреты | Gitleaks: дерево, 93 HEAD commits + 5 side-only refs — PASS после commit |
| Публичный сайт | Метаданные/ссылки локально проверены; публикации нет |

Release builder повторил полный прогон после изменения номера кандидата: PASS.
Gitleaks покрывает доступные локальные refs, не неизвестные форки/кэши.

## Изоляция и диагностика

Свежая копия `NeClip Shot QA` успешно запускается через LaunchServices.
DEBUG QA использует отдельную базу, именованный тестовый clipboard и volatile паузу истории/раскладки даже при перезапуске Finder без переменных окружения.

Старые AX timeout не считаются доказательством зависания: sample показывает штатный idle run loop. Явное удержание weak AppDelegate добавлено как корректная защита времени жизни, но не выдано за доказанную причину всех таймаутов.

При повторной live-проверке формата JPEG автоматизация получила `elementHasNoFrame` / AX timeout; этот системный сценарий пока не засчитан. Файловое ядро JPEG прошло реальные I/O tests.

## Размер

Предыдущий чистый strict release с `strip -S -x`: baseline 4 310 048 байт, кандидат 4 417 840 байт (+107 792; +2.50%). Это размер executable, не DMG. После последних изменений размер нужно освежить. Бинарник после release-тестов с дополнительными символами нельзя использовать для сравнения.

## Установка и оставшиеся gates

Пользователь разрешил Screen Recording тестовой копии и затем явно попросил собрать и установить новую версию в «Программы». В системном списке подтверждён обычный NeClip; запись именно QA-копии не подтверждена. Пароль администратора пользователь вводит только сам в macOS.

Подписанная/notarized сборка установлена в `/Applications/NeClip.app` с исходным bundle ID, после проверки Gatekeeper и с резервной копией прежней 2.2.0. Программа запущена, бинарник совпадает с кандидатом. Инсталлятор восстанавливает старый app при ошибке замены/проверки. Данные не удалялись. Подробности: [RELEASE-2.3.0-STATUS.md](RELEASE-2.3.0-STATUS.md).

Попытка ⌘⇧2 сделана, но получить окно области через CUA не удалось; пользователю отправлен вопрос о видимом результате. Это ещё не подтверждение реального capture и не основание объявлять публичный релиз.

Открыты: реальный SCK кадр и выделение, live JPEG save, разные дисплеи/Spaces и реальные ошибки доступа к папке. Первая версия намеренно ограничена одним дисплеем, 32 MP, SDR/sRGB; HDR, cross-display selection, scrolling и видео не обещаны.

Синтетический PNG для доказательства: `/private/tmp/neclip-screenshot-ui.wwtc5j/screenshot-all-tools.png`.
Исследование: `artifacts/neclip/research/2026-09-10-screenshots/CONSOLIDATED-RESEARCH-RU.md` в родительском workspace.
